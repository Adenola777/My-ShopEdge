fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
stripe>=11.0
psycopg[binary,pool]>=3.2
python-dateutil>=2.9
pyjwt[crypto]>=2.9
httpx>=0.27
# Arrives with pyjwt[crypto] already. Declared in its own right because connections.py uses
# AES-GCM to encrypt the TikTok tokens, and that dependency should not rest on another
# package's extra continuing to include it.
cryptography>=42.0
